export interface WordDocument {
    slug: string;
    label: string;
    definition: string;
    example: string;
    author: string;
    timestamp: number;
    isApproved: boolean;
    ip: string;
}
