import { ReactElement } from "react";

import { Definition as DefinitionComponent } from "../../components/definition"

export const HomePage = (): ReactElement => {
    return (
        <DefinitionComponent id="gyu" />
    );
};
